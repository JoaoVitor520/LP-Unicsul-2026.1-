# Supabase admin automation for dashboard access.
# Keep this file local only. It is gitignored and must not be committed.

SUPABASE_PROJECT_REF=""
SUPABASE_MANAGEMENT_TOKEN=""
SUPABASE_SERVICE_ROLE_KEY=""

# Optional. If omitted, the setup script generates local credentials for the dashboard user.
DASHBOARD_MANAGER_EMAIL=""
DASHBOARD_MANAGER_PASSWORD=""

# Optional. The setup script derives/fetches these if they are absent.
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""
